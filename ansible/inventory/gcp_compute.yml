---
plugin: google.cloud.gcp_compute
projects:
  - ornate-producer-468811-h2
auth_kind: serviceaccount

hostnames:
  - private_ip

keyed_groups:
  - key: labels.role
    prefix: gcp_role

compose:
  monitoring_servers: "labels.role == 'monitoring'"